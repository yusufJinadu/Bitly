{
  "name": "bitly",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-gui-en": "cypress open --config-file en.config.ts",
    "test-headless-en": "cypress run --config-file en.config.ts",
    "format-code": "prettier --config .prettierrc --ignore-path \".gitignore\" --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.3",
    "cypress-mochawesome-reporter": "^3.8.1",
    "cypress-wait-until": "^3.0.1",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  }
}
